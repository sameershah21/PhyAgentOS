# SO-101 Greeting Arm

This runtime profile is copied from `hal/profiles/so101_greeting_arm.md` when
the SO-101 watchdog starts. The checked-in copy exists so the example workspace
is readable before startup.

See: `hal/profiles/so101_greeting_arm.md`
